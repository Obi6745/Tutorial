# Installed packages (do not commit; each machine runs npm install)
node_modules/

# Local secrets (passwords, API keys) if you add a .env file later
.env

# Log files if you write logs to disk
*.log

# Mac system file (not needed in the repo)
.DS_Store

# Instructor-only lab text and answer keys (keep local; not for student clones)
INSTRUCTOR.md
